#include<iostream>
#include<vector>
using namespace std;

int main(int argc, char const *argv[])
{
	/* code */
	int n;
	scanf("%d", &n);
	int m;
	vector<int> v(n);
	int value;
	for (int i = 0; i < n; i++) {
		scanf("%d", &m);
		for (int j = 0; j < m; j++) {
			v[i].push_back();
		}
	}
	
	return 0;
}